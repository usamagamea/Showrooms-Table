import { environment } from "src/environment/environment";

export const apiUrl = `${environment.baseUrl}/showrooms?&page=0&total=50&search=&sort=&sortType`;
export const nestedUrl = `${environment.baseUrl}/requests`;
